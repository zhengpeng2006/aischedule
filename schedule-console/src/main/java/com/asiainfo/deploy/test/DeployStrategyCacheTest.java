package com.asiainfo.deploy.test;

import junit.framework.TestCase;

public class DeployStrategyCacheTest extends TestCase {

	/*public void testCache() {
		HashMap<Long, BODeployStrategyBean> strategys = null;
		try {
			strategys = CacheFactory.getAll(DeployStrategyCache.class);
		} catch (Exception e) {
			fail("get application template error.");
		}

		Set<Map.Entry<Long, BODeployStrategyBean>> set = strategys.entrySet();
		// TODO 待完善 和 直接从数据库中取数据做对比
		for (Map.Entry<Long, BODeployStrategyBean> one : set) {
			System.out.println("[id:" + one.getKey() + ",value:" + one.getValue().getDeployStrategyName() + "]");
		}
	}*/
}
