package com.asiainfo.deploy.test;

import junit.framework.TestCase;

/**
 * 模板脚本缓存测试
 * @author 孙德东(24204)
 */
public class AppScriptCacheTest extends TestCase {

	/*public void testCache() {
		HashMap<Long, AppScript> scripts = null;
		try {
			scripts = CacheFactory.getAll(TemplateScriptCache.class);
		} catch (Exception e) {
			fail("get application template error.");
		}
		
		Set<Map.Entry<Long, AppScript>> set = scripts.entrySet();
		for (Map.Entry<Long, AppScript> one : set)
		{
			System.out.println(one.getKey() + "=" + one.getValue());
		}
	}*/
}
